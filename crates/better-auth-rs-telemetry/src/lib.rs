//! Rust port of `@better-auth/telemetry` (one of better-auth's `packages/`).
//!
//! Not yet ported — the upstream TypeScript sources are co-located in this crate's `src/`
//! (read-only) as the porting spec. This crate exists to anchor the 1:1 package structure;
//! its public API lands in the phase that ports this package.
